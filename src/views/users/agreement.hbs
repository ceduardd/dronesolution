{{> head }}

<div class="wrapper p-8 border rounded mt-8">
  <div class="flex justify-end items-center">
    <img src="/assets/img/ds-logo.svg" alt="logo" width="50" />
    <p class="font-bold text-lg">DroneSolution</p>
  </div>
  <h1
    class="text-center text-3xl mb-8 font-bold text-bold font-secondary uppercase"
  >
    Contrato <i class="fas fa-file-signature"></i>
  </h1>
  <p class="font-secondary mb-4">Fecha: {{ formatDate details.DATE_ISSUE }}</p>
  <p></p>
  <p class="font-secondary mb-4">
    Yo {{ details.FULLNAME }} con cédula de identidad
    <span class="font-bold">{{ details.DNI }}</span> contrato los servicios de
    DroneSolution para el control de mi evento
    <span class="font-bold">{{ details.NAME }}</span> con fecha prevista para el
    <span class="font-bold">{{ formatDate details.DATE_START }}</span> a las
    <span class="font-bold">{{ formatHour details.DATE_START }}</span> horas.
  </p>
  <h2 class="font-secondary mb-2">Detalles del servicio contratado:</h2>
  <ul>
    <li>Tipo plan: {{ details.NAME_PLAN }}</li>
    <li>Valor: $ {{ details.PRICE }}</li>
  </ul>
</div>

{{> back }}

{{!-- SOCKET.IO --}}
<script src="/socket.io/socket.io.js"></script>
<script src="/js/socket.auth.js"></script>
